{
  "name": "nationlabs-orchestrator",
  "version": "1.0.0",
  "private": true,
  "description": "NationLabs AI Presales Orchestrator — FastAPI prototype (Python backend served via uvicorn)",
  "scripts": {
    "dev": "python -m uvicorn orchestrator.web.app:app --host 0.0.0.0 --port 7100"
  }
}
