nemoguardrails==0.23.0  # Pinned 26-Jul-2026: reasoning-trace visibility patch
                        # (config_uat/default/actions.py) targets this exact
                        # version's internals (nemoguardrails.server.api's
                        # generation_response_to_chat_completion binding). An
                        # unpinned range risked a future rebuild silently
                        # breaking or no-op'ing the patch. Bump deliberately and
                        # re-verify the patch (see RUNBOOK.md Section 6.9) before
                        # moving this pin.
litellm
uvicorn
fastapi

# Phase 3 follow-up (21-Jul-2026): audit logging - ship guardrail decision
# logs to Loki via OpenTelemetry's Python auto-instrumentation. See
# docker-compose.yml OTEL_* env vars and Dockerfile CMD for the wiring.
opentelemetry-distro[otlp]
opentelemetry-exporter-otlp-proto-http
